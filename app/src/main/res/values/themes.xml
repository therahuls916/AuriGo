<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- This is your main app theme -->
    <style name="Theme.AuriGo" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- This is the theme for the system splash screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- The background color of the splash screen -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- The icon to display on the splash screen -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>

        <!-- The theme to apply AFTER the splash screen is gone -->
        <item name="postSplashScreenTheme">@style/Theme.AuriGo</item> <!-- CORRECTED NAME -->
    </style>

</resources>